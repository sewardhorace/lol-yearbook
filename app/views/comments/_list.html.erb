
<% if comments.count > 0 %>
  <% comments.order(created_at: :desc).each do |c| %>
    <%= render partial: "comments/comment", locals: {comment: c}%>
  <% end %>
<% else %>
  <p class="text-center">No notes yet...</p>
<% end %>
