<div class="row">

  <div class="col-xs-12" id="_5">
    <h4 class="grade-label">Seniors</h4>
  <% champions.where(mastery_level: 5).order(mastery_points: :desc).each do |champ| %>
    <%= render partial: "champion_thumb", locals:{champion: champ, url: champion_path(region, champ.summoner_id, champ.champion_id)} %>
  <% end %>
  </div>

  <div class="col-xs-12" id="_4">
    <h4 class="grade-label">Juniors</h4>
  <% champions.where(mastery_level: 4).order(mastery_points: :desc).each do |champ| %>
    <%= render partial: "champion_thumb", locals:{champion: champ, url: champion_path(region, champ.summoner_id, champ.champion_id)} %>
  <% end %>
  </div>

  <div class="col-xs-12" id="_3">
    <h4 class="grade-label">Sophomores</h4>
  <% champions.where(mastery_level: 3).order(mastery_points: :desc).each do |champ| %>
    <%= render partial: "champion_thumb", locals:{champion: champ, url: champion_path(region, champ.summoner_id, champ.champion_id)} %>
  <% end %>
  </div>

  <div class="col-xs-12" id="_2">
    <h4 class="grade-label">Freshmen</h4>
  <% champions.where(mastery_level: 2).order(mastery_points: :desc).each do |champ| %>
    <%= render partial: "champion_thumb", locals:{champion: champ, url: champion_path(region, champ.summoner_id, champ.champion_id)} %>
  <% end %>
  </div>

</div>
